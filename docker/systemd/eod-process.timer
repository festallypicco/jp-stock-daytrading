[Unit]
Description=Run eod-process daily on trading days

[Timer]
# OnCalendar=Mon-Friは土日を除外するが祝日は除外しないため、祝日もタイマーは発火する。
# run_eod_process内部のis_trading_day()（曜日・年末年始・国民の祝日）で空振りする。
OnCalendar=Mon-Fri 15:15:00 Asia/Tokyo
Persistent=false

[Install]
WantedBy=timers.target
