[Unit]
Description=Run morning-trade daily on trading days

[Timer]
# 祝日カレンダー対応は別タスクの範囲外。OnCalendar=Mon-Friは土日を除外するが
# 祝日は除外しないため、祝日も起動されバッチ内部のis_trading_day()判定
# （現状は曜日判定のみ）で空振りする、意図的な暫定挙動。
OnCalendar=Mon-Fri 08:55:00 Asia/Tokyo
Persistent=false

[Install]
WantedBy=timers.target
